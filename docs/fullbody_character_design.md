# 阶段 4 历史记录：完整人物候选设计

阶段 4 曾生成方案 A、B、C 三套全身候选。用户现已正式选择 **方案 B**；A、C 保留为只读历史候选，不能在运行时替代或拼接到方案 B。

方案 B 的已确认识别特征包括：深色不对称长发与厚刘海、右向发丝、粉紫大眼睛、温和表情、深色主体服装、浅灰紫袖口、双层深色裙装、红色腰带和裙边、深灰长袜、带红色扣件的深色短靴，以及胸前持信的双手姿势。

阶段 4 的候选文件、比较图和元数据继续保留在 `assets/fullbody/concepts/` 与 `assets/fullbody/reports/fullbody_concept_manifest.json`，仅用于可追溯性。正式运行时只能加载 `assets/fullbody/final/fullbody_runtime_master.png`。
