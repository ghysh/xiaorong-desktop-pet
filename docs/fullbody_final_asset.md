# 阶段 5：方案 B 最终全身运行素材

用户已选择并视觉确认方案 B。正式主图为 `assets/fullbody/final/fullbody_runtime_master.png`，尺寸为 1024 × 1536、RGBA、2:3，SHA-256 为 `6FD2E4CA948E250926A22428AA633AF83F487971086ABA92B1017C3599747A64`。

该主图、方案 B 候选和只读选中副本具有相同 SHA-256，代表文件级无损复制；未重绘角色、未改动 Alpha、未做颜色调整，也未修改原图、第三阶段参考素材或 A/C 候选。

阶段 5 的透明检查确认四侧均有安全边距、疑似错误孔洞为 0、可疑洋红残留为 0，280 × 420 下双层裙摆仍可辨认。诊断、预览和完整清单分别位于 `assets/fullbody/diagnostics/`、`assets/fullbody/previews/` 和 `assets/fullbody/reports/fullbody_final_asset_manifest.json`。

阶段 6 的静态窗口原型仅加载该主图。上半身参考素材不再作为运行时主图；动画、状态机、托盘和设置仍未实现。
