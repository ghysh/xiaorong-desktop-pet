# 角色素材视觉规范（第二阶段草案）

> 状态：**草案，待用户确认**。本规范只描述原图的视觉约束与后续制作风险；不构成抠图、重绘、补帧或动画制作指令。

## 1. 原图信息

- 来源：`D:\DesktopPet\ori_figure.png`
- 项目内只读副本：`assets/original/character_original.png`
- 完整性：SHA-256 为 `CCF0AABC6D1DD7AFF61590E40BBEF7C0E2411B6524CF47C72D6BC10BDE900DB3`
- 技术信息：PNG、346 × 346、RGB、无 Alpha、长宽比 1.0000。
- 分析产物：`assets/analysis/previews/` 与 `assets/analysis/reports/character_analysis.json`。

## 2. 角色总体描述

- **观察**：女性化日系二次元人物近景，正面略偏侧的上半身构图；头部、粉紫调大眼睛与靠近下颌的双手构成视觉重心。
  - 观察依据：直接查看原图和三倍放大预览。
  - 确定程度：高。
  - 后续必须保持：是。
- **观察**：深色长发、红色点缀与深色服装形成主轮廓和高对比视觉节奏。
  - 观察依据：原图与自动配色统计。
  - 确定程度：高。
  - 后续必须保持：是。

## 3. 核心辨识特征

### A 级：绝对不能改变

| 特征 | 分类依据 | 确定程度 |
| --- | --- | --- |
| 深色不对称长发、厚重刘海、向右展开的发丝轮廓 | 构成最大、最独特的外轮廓 | 高 |
| 粉紫调大眼睛、柔和面颊晕染与温和表情 | 面部是当前构图的主要视觉重心 | 高 |
| 深色服装与红色点缀的高对比关系 | 与发色共同建立稳定色彩识别 | 中 |

### B 级：原则上保持

| 特征 | 分类依据 | 确定程度 |
| --- | --- | --- |
| 下颌前双手靠拢的近景姿势 | 当前静态形象的重要姿势；低改动动画可轻微变化 | 高 |
| 红色头部点缀、深色背带与扣件的相对关系 | 可因姿势变形，但需保留位置关系和视觉权重 | 中 |
| 头大身小的近景视觉比例 | 是当前头像式桌宠形象的构图基础 | 高 |

### C 级：允许调整

| 特征 | 分类依据 | 确定程度 |
| --- | --- | --- |
| 白色/近白背景与周围留白 | 不是角色身份组成；后续可在用户确认后处理为透明 | 高 |
| 画外身体、鞋袜、完整服装结构 | 单张近景无法确认；任何补全均为待用户确认内容 | 高 |
| 极细瞳孔、眉毛、鼻部和小饰物结构 | 346 × 346 下不宜作为精确复刻依据 | 高 |

## 4. 主色与辅助色

以下为 Pillow 的 12 色量化统计；用途均为保守推测，**待视觉确认**。

| 色值 | RGB | 占比 | 保守用途 |
| --- | --- | ---: | --- |
| `#FFFFFF` | 255, 255, 255 | 23.27% | 可能属于背景 |
| `#28252A` | 40, 37, 42 | 12.76% | 可能属于轮廓或阴影 |
| `#43363B` | 67, 54, 59 | 11.45% | 可能属于轮廓或阴影 |
| `#FAF9FA` | 250, 249, 250 | 10.69% | 可能属于背景或高光 |
| `#C3BBC3` | 195, 187, 195 | 8.00% | 可能属于角色主体 |
| `#545056` | 84, 80, 86 | 7.29% | 可能属于轮廓或阴影 |
| `#E8E2E6` | 232, 226, 230 | 6.92% | 待视觉确认 |
| `#FDFEFE` | 253, 254, 254 | 6.42% | 可能属于背景 |

- 观察依据：`assets/analysis/reports/character_analysis.json` 的量化统计与原图视觉复核。
- 确定程度：颜色数值为高；颜色归属为低至中。
- 后续必须保持：深色主体、红色点缀、浅色面部/背景之间的对比关系为是；精确色值待用户确认。

## 5. 轮廓规则

- 必须保持厚重刘海、左侧束发和向右外扩的长发丝构成的不对称上半身轮廓。
  - 观察依据：原图和构图网格图。
  - 确定程度：高。
  - 后续必须保持：是。
- 当前算法估计主体范围为 `x=0, y=11, 346 × 335`，触及左、右、下边缘；这是**主体范围初步估计**，不是精确分割结果。
  - 观察依据：与边缘背景候选 `#FFFFFF` 的 RGB 距离阈值统计。
  - 确定程度：低。
  - 后续必须保持：否；仅用于提示动作扩展留白不足。

## 6. 面部规则

- 需保持大而圆润的粉紫调眼睛、深色上睫线、柔和腮红、细小鼻口与内敛温和的表情关系。
  - 观察依据：直接查看原图和三倍放大预览。
  - 确定程度：中。
  - 后续必须保持：是。
- 不应凭当前图像精确重建瞳孔纹理、眉形或鼻口像素细节。
  - 观察依据：原图分辨率和前景手部遮挡。
  - 确定程度：高。
  - 后续必须保持：**待用户确认**。

## 7. 服装和装饰规则

- 可见部分应保持深灰至黑色服装、浅灰/淡紫袖口、红色绑带/扣件及头部红色点缀。
  - 观察依据：原图直接观察。
  - 确定程度：中。
  - 后续必须保持：是。
- 服装完整结构、鞋袜、画外身体与细小饰物结构不可由当前单张近景确认。
  - 观察依据：画面裁切。
  - 确定程度：高。
  - 后续必须保持：**待用户确认**。

## 8. 线条和上色规则

- 保持日系二次元插画的深色轮廓、软渐变阴影、面部暖色晕染、细发丝高光和抗锯齿边缘。
  - 观察依据：直接查看三倍缩放预览。
  - 确定程度：高。
  - 后续必须保持：是。
- 后续不应将角色简化为硬边扁平矢量风，也不应以锐化或高对比滤镜替代原有软过渡。
  - 观察依据：原图明暗和边缘过渡。
  - 确定程度：高。
  - 后续必须保持：是。

## 9. 禁止改变的特征

1. 深色长发、厚刘海与右向发丝的总体轮廓。
2. 粉紫调大眼睛、柔和脸部晕染和温和表情倾向。
3. 深色主服装与红色点缀的对比关系。
4. 当前插画式柔和渐变、细发丝高光和非硬边线稿风格。

以上均来自原图直接观察；第 1、2、4 项为高确定程度，第 3 项为中确定程度。完整 A 级清单仍为**待用户确认**。

## 10. 允许因动画产生的变化

- 可在不改写 A 级特征的前提下，使用整体位移、轻微缩放、轻微倾斜和轻微呼吸形变。
- 眨眼、嘴部/表情微调、头部转动和手部动作需要局部补绘；当前仅为方案，**待用户确认**。
- 行走、坐下、睡觉、转身等需要完整重绘或多帧动画；本阶段不制作。

## 11. 默认显示尺寸建议

- 默认候选：`240 × 240`，为原图的 69.4% 缩小；保留较好可读性，桌面占用属中等。
- 备选：`280 × 280`，为 80.9% 缩小；细节可见度更好，但桌面占用更大。
- 最终默认尺寸：**待用户确认**。请参照 `assets/analysis/previews/character_size_comparison.png`。

## 12. 高清缩放限制

- `160`、`200`、`240`、`280`、`320` 均是原图缩小，不会凭缩放增加细节。
- 不建议将现有单张 346 × 346 图像放大为高于原生尺寸后当作高清素材。
- 原图为近景且主体触边，完整动作和更大构图需要新的经确认绘制资产。

## 13. 背景处理建议

- 推荐：**方案 B，半自动蒙版与人工修边**；后续若需完整动画，可演进为方案 D。
- 依据：三角和顶部边缘接近 `#FFFFFF`，但底边代表色为 `#958E92`，算法估计主体触及左右和下边缘，且浅色服装/高光与背景接近。
- 观察依据：`character_analysis.json`、构图图和原图直接观察。
- 确定程度：中；算法对主体范围的置信度为低。
- 本阶段状态：未去背景、未生成蒙版。

## 14. 待用户确认事项

1. 是否认可 A 级不可改变特征清单。
2. 默认尺寸选择 `240 × 240` 还是 `280 × 280`。
3. 是否采用方案 B 背景处理，并在后续阶段允许向方案 D 演进。
4. 允许后续局部重绘和补帧的范围。
5. 当前角色称谓、画外服装与身体结构是否有额外参考资料或指定要求。

## 第三阶段执行基线（透明基础素材候选）

本节仅记录已确认的执行边界，不替代用户对最终视觉的确认。

- 使用方案 B：本地半自动掩码、人工精细修边与透明边缘去污染；不调用生成式 AI 或在线服务。
- 仅允许处理背景、Alpha、1–3 像素边缘和透明留白；不改变不透明角色内部的绘制内容。
- 必须保留 A 级特征：深色不对称长发、厚刘海、右向发丝、粉紫大眼、柔和腮红、温和表情、深色服装与红色点缀，以及柔和渐变和细发丝高光。
- 默认逻辑显示尺寸为 240 × 240，280 × 280 为备选；410 × 410 运行时主图只增加透明安全边距，不缩放原始像素。
- 动作范围仍为选项 1：后续仅可研究整体位移、缩放、轻微倾斜或呼吸式变换；本阶段不制作动作帧。
- 透明边缘候选仍需用户确认后，才能进入透明窗口原型阶段。
